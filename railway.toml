[build]
builder = "nixpacks"

[deploy]
startCommand = "curl -fsSL -o /usr/local/bin/lightpanda https://github.com/lightpanda-io/browser/releases/download/nightly/lightpanda-x86_64-linux && chmod +x /usr/local/bin/lightpanda && python bot.py"
restartPolicyType = "always"
restartPolicyMaxRetries = 3
